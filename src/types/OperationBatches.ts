/**
 * Represents an array of arrays of `Operation` objects.
 * Each inner array represents a batch of operations.
 */

import { Operation } from './Operation';

export type OperationBatches = Operation[][];
